---
- hosts: all
  gather_facts: false
  tasks:
    - name: Display parsed parameters
      debug:
        msg: 
          - "Param1: {{ ansible_facts.param1 }}"
          - "Param2: {{ ansible_facts.param2 }}"
          - "Param3: {{ ansible_facts.param3 }}"